package io.github.srvenient.elegantoptions.api;

public enum Enums {;

    public enum TypeStatus {
        ON,
        OFF,
        ONLY_RANK,
        NO_PERMISSION,
    }

    public enum TypeDatabase {
        MySQL,
        SQLite,
    }
}
